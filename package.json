{
  "name": "banrural-prueba-tecnica",
  "version": "1.0.0",
  "description": "Proyecto de prueba técnica para Banrural con Cypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "clean": "rm -rf node_modules package-lock.json && npm cache clean --force",
    "install:clean": "npm run clean && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.15.0"
  },
  "dependencies": {
    "lodash": "^4.17.21", 
    "chalk": "^4.1.2"
  }
}
